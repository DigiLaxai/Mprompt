import React from 'react';

/**
 * @deprecated This component is no longer in use.
 * The application now uses the API_KEY from environment variables for security and simplicity.
 */
export const ApiKeySelector: React.FC = () => {
    return null;
};
