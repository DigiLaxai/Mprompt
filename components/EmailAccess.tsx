import React from 'react';

/**
 * @deprecated This component is no longer in use.
 * The application now uses the API_KEY from environment variables, removing the need for this access step.
 */
export const EmailAccess: React.FC = () => {
    return null;
};
