
import React from 'react';

// This component is no longer used.
// The application now relies on a backend proxy, and the API key is managed on the server
// via environment variables. This prevents exposing the key on the client-side.
export const ApiKeyModal: React.FC = () => null;
