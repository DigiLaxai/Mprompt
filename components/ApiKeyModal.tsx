import React from 'react';

// This component is intentionally left empty to remove the UI for entering an API key.
// API keys must be managed via environment variables (process.env.API_KEY)
// as per the application's requirements. This change ensures compliance and
// resolves user confusion about API key entry.
export const ApiKeyModal: React.FC = () => null;
