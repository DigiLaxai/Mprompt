import React from 'react';

// This component is intentionally left blank.
// The application now relies exclusively on environment variables for the API key,
// and no longer prompts the user for configuration.
export const ApiKeyPrompt: React.FC = () => null;
